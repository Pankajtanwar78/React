import { StyledError } from "../styles/Quiz.styles";

const Error = () => {
  return (
    <StyledError>
      <span>💥</span> There was an error fecthing questions.
    </StyledError>
  );
}

export default Error;
